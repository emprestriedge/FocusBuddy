node_modules/
dist/
.netlify/
.DS_Store
.env
.env.*
!.env.example
*.p8

# Prompt/chat history — never commit these, they can contain API keys
migrated_prompt_history/
prompt_history/
prompt_*.json
*_prompt_history*
chat_history/

# Netlify deploy zips (contain built dist/ with baked-in keys)
Deploy/
deploy-*.zip
focusbuddy-deploy.zip
ziUCby3p
